defaults:
  - _self_
  - aloha_env: 0121_bottle_on_rack

suite: aloha_env

task_name: ${suite.task.aloha_env.task_name}
object_labels: ${suite.task.aloha_env.object_labels}
num_object_points: ${suite.task.aloha_env.num_object_points}

root_dir: ${suite.task.aloha_env.root_dir}
prompts: ${suite.task.aloha_env.prompts}
